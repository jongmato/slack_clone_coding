import Workspace from '@layouts/Workspace/index';
import React from 'react';

const Channel = () => {
    return (
        <Workspace>
            <div>hi</div>
        </Workspace>
    );
};

export default Channel;
